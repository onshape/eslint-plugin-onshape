{
  "name": "eslint-plugin-onshape",
  "version": "0.1.3",
  "description": "Onshape eslint plugin",
  "keywords": [
    "eslint",
    "eslintplugin",
    "eslint-plugin"
  ],
  "author": "Mark Barr",
  "main": "lib/index.js",
  "scripts": {
    "lint": "./node_modules/.bin/eslint lib/**/*.js || exit 0",
    "fix": "./node_modules/.bin/eslint --fix lib/**/*.js || exit 0"
  },
  "dependencies": {
    "requireindex": "^1.2.0"
  },
  "devDependencies": {
    "eslint": "^7.3.1"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "license": "ISC"
}
